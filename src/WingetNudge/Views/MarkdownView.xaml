<UserControl
    x:Class="WingetNudge.Views.MarkdownView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
>
    <Grid>
        <WebView2 x:Name="Browser" NavigationStarting="OnNavigationStarting" />
        <ScrollViewer x:Name="FallbackScroller" Visibility="Collapsed">
            <TextBlock x:Name="Fallback" TextWrapping="Wrap" IsTextSelectionEnabled="True" />
        </ScrollViewer>
    </Grid>
</UserControl>
