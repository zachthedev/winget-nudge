# Line endings are LF on every machine, whatever a contributor's core.autocrlf says. The git hooks
# run under sh, which rejects a carriage return at the end of a line.
* text=auto eol=lf

*.ico binary
*.png binary
